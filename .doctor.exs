%Doctor.Config{
  min_overall_spec_coverage: 0,
  struct_type_spec_required: false,
  ignore_modules: [
    Mneme.Schema.Chunk,
    Mneme.Schema.Collection,
    Mneme.Schema.Document,
    Mneme.Schema.Edge,
    Mneme.Schema.Entity,
    Mneme.Schema.Entry,
    Mneme.Schema.PipelineRun,
    Mneme.Schema.Relation,
    Mneme.TestRepo
  ]
}
